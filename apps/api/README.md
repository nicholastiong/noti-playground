# API

Fastify TypeScript backend for `noti-playground`.

## Development

```bash
pnpm dev:api
```

The app runs on `http://localhost:4000` by default.

## Endpoints

- `GET /healthz`
- `GET /version`

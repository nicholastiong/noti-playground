{
  "name": "@noti-playground/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "dotenv -e .env -- tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "dotenv -e .env -- node dist/server.js",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "vapid:keys": "tsx src/scripts/generate-vapid-keys.ts"
  },
  "dependencies": {
    "@fastify/cors": "latest",
    "fastify": "latest",
    "web-push": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "latest",
    "@types/web-push": "latest",
    "dotenv-cli": "latest",
    "eslint": "^9.0.0",
    "tsx": "latest",
    "typescript": "latest",
    "typescript-eslint": "latest",
    "vitest": "latest"
  }
}
